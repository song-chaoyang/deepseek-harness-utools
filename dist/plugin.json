{
  "main": "index.html",
  "logo": "logo.png",
  "preload": "preload.js",
  "development": {
    "main": "index.html"
  },
  "features": [
    {
      "code": "dsh-main",
      "explain": "DeepSeek Harness — AI Agent 工作台（启动服务器、打开 Web UI）",
      "cmds": ["DeepSeek Harness", "dsh", "DSH", "deepseek harness", "AI Agent"]
    },
    {
      "code": "dsh-sessions",
      "explain": "浏览会话历史（搜索、Fork、取消、截图发送、导出）",
      "cmds": ["dsh sessions", "DSH 会话", "dsh history"]
    },
    {
      "code": "dsh-plugin-mgr",
      "explain": "管理 DSH 插件（安装/卸载/搜索）",
      "cmds": ["dsh plugins", "DSH 插件管理", "dsh plugin"]
    },
    {
      "code": "dsh-workspace",
      "explain": "选择工作区目录并在 DSH 中打开",
      "cmds": ["dsh workspace", "DSH 工作区"],
      "platform": ["win32", "darwin", "linux"]
    },
    {
      "code": "dsh-goals",
      "explain": "管理会话目标（创建/编辑/暂停/恢复/完成）",
      "cmds": ["dsh goals", "DSH 目标", "dsh goal"]
    }
  ],
  "tools": {
    "dsh_run_headless": {
      "description": "使用 DeepSeek Harness 执行一个 AI 编程任务",
      "inputSchema": {
        "type": "object",
        "properties": {
          "task": {
            "type": "string",
            "description": "任务描述，例如：分析项目架构"
          },
          "workspace": {
            "type": "string",
            "description": "工作区路径（可选，默认使用当前工作区）"
          }
        },
        "required": ["task"]
      }
    },
    "dsh_list_sessions": {
      "description": "列出所有 DSH 会话历史",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "搜索关键词（可选）"
          }
        }
      }
    },
    "dsh_read_file": {
      "description": "读取指定路径的文件内容",
      "inputSchema": {
        "type": "object",
        "properties": {
          "path": {
            "type": "string",
            "description": "文件绝对路径"
          }
        },
        "required": ["path"]
      }
    },
    "dsh_search_sessions": {
      "description": "在 DSH 会话历史中搜索内容",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": {
            "type": "string",
            "description": "搜索词"
          }
        },
        "required": ["query"]
      }
    },
    "dsh_get_server_status": {
      "description": "获取 DSH 服务器运行状态",
      "inputSchema": {
        "type": "object",
        "properties": {}
      }
    }
  }
}
